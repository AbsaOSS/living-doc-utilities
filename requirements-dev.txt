# Development tooling: everything `make qa` and CI need on top of the runtime list.
-r requirements.txt

# Libraries behind the optional extras (pyproject.toml), so the tests can import
# `github/` (the `github` extra) and exercise the HTML sanitiser (the `html` extra).
PyGithub==2.10.0
requests==2.34.2
nh3==0.3.7

# Static analysis, formatting and type checking
mypy==2.3.1
mypy-extensions==1.1.0
typing_extensions==4.16.0
pylint==4.0.8
ruff==0.16.8
black==26.5.1
deptry==0.25.1
types-requests==2.33.0.20260906
types-jsonschema==4.26.0.20260518
types-PyYAML==6.0.12.20260906

# Tests
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1

# Wheel build for the import matrix (`make import-matrix`)
build==1.6.1
